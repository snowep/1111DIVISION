[project]
name = "jarvis"
version = "0.1.0"
description = "JARVIS — Personal Intelligence Operating System"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
