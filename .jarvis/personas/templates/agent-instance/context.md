# Agent Context (template)

> Input state — what is happening. Written by JARVIS at instance creation.

## Situation

<What is happening right now — the task environment.>

## Inputs

<Relevant files, decisions, conversations, or references the instance needs.>

## Constraints

<Explicit limits. The instance operates within these and nothing else.>

## Notes

- This context is isolated per instance.
- It does not persist after the instance is archived or deleted.